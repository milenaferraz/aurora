# Porta do serviço gateway do Hermes (padrão: 8642)
# Confirme em: cat ~/.hermes/.env | grep PORT
HERMES_PORT=8642

# Chave de autenticação do Hermes (API_SERVER_KEY em ~/.hermes/.env)
HERMES_API_KEY=

# Modelo a usar (opcional, padrão: hermes)
HERMES_MODEL=hermes
